[project]
name = "wallpha"
version = "2.2.1"
description = "Wallpaper animado/imagem no KDE Plasma com modo automático"
requires-python = ">=3.10"
dependencies = [
    "dbus-python>=1.3",
    "pyyaml>=6.0",
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
include = ["wallpha*"]

[tool.setuptools.package-data]
wallpha = ["assets/*"]

[project.scripts]
wallpha = "wallpha:main"

[tool.pytest.ini_options]
testpaths = ["tests"]

[dependency-groups]
dev = [
    "pytest (>=8.0.0,<10.0.0)"
]